{
  "name": "backlog-models",
  "version": "1.0.2",
  "description": "All models used in Backlog's MongoDB",
  "main": "lib/index.js",
  "scripts": {
    "dev": "nodemon --exec npm run compile",
    "compile": "babel --presets es2015,stage-0 -d lib/ src/",
    "prepublish": "npm run compile",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "vlad.barboni@gmail.com",
  "license": "ISC",
  "dependencies": {
    "bluebird": "3.5.0",
    "meteor-random": "0.0.3",
    "mongoose": "4.9.3"
  },
  "devDependencies": {
    "babel-cli": "6.14.0",
    "babel-preset-es2015": "6.14.0",
    "babel-preset-stage-0": "6.5.0"
  }
}
